{
  "name": "simplegov",
  "version": "1.0.0",
  "description": "SimpleGov",
  "scripts": {
    "dev": "docker-compose up",
    "dev:reset": "sh docker-reset.sh && docker-compose up --build --remove-orphans --force-recreate",
    "database": "docker-compose up simplegov-postgres simplegov-hasura"
  },
  "author": "Me",
  "license": "Commercial",
  "dependencies": {
    "prop-types": "^15.8.1"
  },
  "devDependencies": {
    "@types/node": "^18.15.0"
  }
}
